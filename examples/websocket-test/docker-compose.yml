name: ignis-example-websocket

services:
  redis:
    image: redis:7-alpine
    restart: on-failure
    ports:
      - '16381:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10
