# Mail Component

Flexible email sending system with support for multiple transports (Nodemailer, Mailgun, custom), template-based rendering with `{{variable}}` syntax, and queue-based processing via Direct, Internal Queue, or BullMQ executors.
