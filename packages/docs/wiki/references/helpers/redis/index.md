# Redis Helper

Powerful Redis abstraction supporting single instances and clusters via `ioredis`. Provides automatic JSON serialization, connection lifecycle callbacks, Pub/Sub with optional compression, RedisJSON operations, and raw command execution.
