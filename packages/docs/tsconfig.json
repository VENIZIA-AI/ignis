{
  "$schema": "http://json.schemastore.org/tsconfig",
  "//": "Config that Vite/esbuild reads for the VitePress site. Standalone (does NOT extend the ES2024 dev-configs base) because esbuild rejects the ES2024 target. The MCP server build uses tsconfig.mcp.json, which extends the base and keeps ES2024.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["mcp-server/**/*.ts", "site/**/*.ts", "site/**/*.mts"],
  "exclude": ["node_modules", "dist", "mcp-server/dist", "wiki"]
}
