# No Frontmatter Title

This document has no frontmatter block at all, so its title must come from the first level-one
heading instead of falling back to the file path.

## Details

The loader reads this heading when `data.title` is missing, not just when frontmatter is invalid.
