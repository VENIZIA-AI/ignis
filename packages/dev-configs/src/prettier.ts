import type { Config } from "prettier";

export const prettierConfigs: Config = {
  bracketSpacing: true,
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  trailingComma: "all",
  arrowParens: "avoid",
  semi: true,
};
