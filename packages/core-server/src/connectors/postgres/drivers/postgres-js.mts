/** ESM twin of `postgres-js.ts`, served to `import`: the CommonJS chain `require()`s `postgres`, which Bun refuses while an ESM importer of `postgres` is still loading. */
export * from '@venizia/ignis-connectors/postgres/postgres-js';
