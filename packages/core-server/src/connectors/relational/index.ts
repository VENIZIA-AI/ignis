/** Alias barrel - the connectors moved to `@venizia/ignis-connectors`. Kept so `@venizia/ignis/relational` keeps resolving for published consumers. */
export * from '@venizia/ignis-connectors/relational';
