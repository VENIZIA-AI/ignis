# Test environment config (loaded via `bun test --env-file=.env.test`, see package.json).
# Keep log + audit files inside app_data — mirroring the real service — instead of
# scattering them at the package root. app_data/ is gitignored, so these stay local.
APP_ENV_LOGGER_FOLDER_PATH=./app_data/logs
